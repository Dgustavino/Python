var animales = ["perro", "gato", "pez"];
// Array

// objetos {dict} == {Obj}

// length es una propiedad, no una funcion
var totalAnimales = animales.length;

// ejemplo de for para recorrer el objeto iterable
for (var i = 0; i < totalAnimales; i++) {
    console.log(animales[i]);
};